<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<style> 
/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #f4f4f9;
    color: #333;
    line-height: 1.6;
    padding: 20px;
}

/* Form Container */
.form-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 20px;
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

h2, h3, h4 {
    color: #2c3e50;
    margin-bottom: 15px;
}

h2 {
    font-size: 28px;
    text-align: center;
    margin-bottom: 25px;
}

h3 {
    font-size: 22px;
    margin-top: 20px;
    border-bottom: 2px solid #3498db;
    padding-bottom: 10px;
}

h4 {
    font-size: 18px;
    margin-bottom: 10px;
    color: #3498db;
}

/* Grid Container */
.grid-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 15px;
    margin-bottom: 20px;
}

/* Form Inputs */
label {
    display: block;
    font-weight: 500;
    margin-bottom: 5px;
    color: #555;
}

input[type="text"],
input[type="date"],
input[type="number"],
input[type="file"],
select,
textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 5px;
    font-size: 14px;
    margin-bottom: 10px;
    transition: border-color 0.3s ease;
}

input[type="text"]:focus,
input[type="date"]:focus,
input[type="number"]:focus,
input[type="file"]:focus,
select:focus,
textarea:focus {
    border-color: #3498db;
    outline: none;
}

/* Checkbox Group */
.checkbox-group {
    margin-bottom: 20px;
    
}

/* Style for the label container */
.checkbox-group label {
    display: flex; /* Make the label a flex container */
    justify-content: space-between; /* Push the select to the right */
    align-items: center; /* Vertically center the select */
    padding: 10px 0; /* Add some padding for spacing */
    border-bottom: 1px solid #eee; /* Optional: Add a border between items */
}

/* Style for the select dropdown */
.checkbox-group select {
    width: 150px; /* Set a fixed width for the dropdown */
    padding: 8px; /* Add padding for better appearance */
    border: 1px solid #ccc; /* Add a border */
    border-radius: 4px; /* Rounded corners */
    font-size: 14px; /* Adjust font size */
}
.checkbox-group input[type="checkbox"] {
    margin-right: 20px;
}

select {
    width: 150px; 
    padding: 8px; 
    border: 1px solid #ccc;
  }

/* File Upload */
.file-upload {
    margin-bottom: 20px;
}

.file-upload label {
    font-weight: 500;
    color: #555;
}

/* Submit Button */
input[type="submit"] {
    display: block;
    width: 100%;
    padding: 12px;
    background: #3498db;
    color: #fff;
    border: none;
    border-radius: 5px;
    font-size: 16px;
    cursor: pointer;
    transition: background 0.3s ease;
}

input[type="submit"]:hover {
    background: #2980b9;
}

/* Responsive Design */
@media (max-width: 768px) {
    .grid-container {
        grid-template-columns: 1fr;
    }

    h2 {
        font-size: 24px;
    }

    h3 {
        font-size: 20px;
    }

    h4 {
        font-size: 16px;
    }
}

@media (max-width: 480px) {
    body {
        padding: 10px;
    }

    .form-container {
        padding: 15px;
    }

    input[type="text"],
    input[type="date"],
    input[type="number"],
    input[type="file"],
    select,
    textarea {
        font-size: 12px;
    }

    input[type="submit"] {
        font-size: 14px;
    }
}

</style>
    <title>Inspection Form</title>
      
</head>
<body>
    <div class="form-container">
        <h2>Plumbing Report</h2>

        <form id="plumbingForm" action="/submit-plumbing-report" method="POST" enctype="multipart/form-data">
            <!-- Inspection & Customer Details -->
            <div class="grid-container">
                <div>
                    <label for="inspectionDate">Inspection Date:</label>
                    <input type="date" id="inspectionDate" name="inspectionDate" value="<%= formData.inspectionDate %>" required>
                </div>
                <div>
                    <label for="inspectorName">Inspector Name:</label>
                    <input type="text" id="inspectorName" name="inspectorName" value="<%= formData.inspectorName %>" required>
                </div>
                <div>
                    <label for="customerName">Customer Name:</label>
                    <input type="text" id="customerName" name="customerName" value="<%= formData.customerName %>" required>
                </div>
                <div>
                    <label for="customerAddress">Customer Address:</label>
                    <input type="text" id="customerAddress" name="customerAddress" value="<%= formData.customerAddress %>" required>
                </div>
                <div>
                    <label for="contactNumber">Contact Number:</label>
                    <input type="text" id="contactNumber" name="contactNumber" value="<%= formData.contactNumber %>" required>
                </div>
            </div>

            <!-- Property Information -->
            <h3>Property Information</h3>
            <div class="grid-container">
                <div>
                    <label for="propertyType">Property Type:</label>
                    <select id="propertyType" name="propertyType">
                        <option value="Residential" <%= formData.propertyType === 'Residential' ? 'selected' : '' %>>Residential</option>
                        <option value="Commercial" <%= formData.propertyType === 'Commercial' ? 'selected' : '' %>>Commercial</option>
                    </select>
                </div>
                <div>
                    <label for="bathrooms">Number of Bathrooms:</label>
                    <input type="number" id="bathrooms" name="bathrooms" value="<%= formData.bathrooms %>" min="1">
                </div>
                <div>
                    <label for="kitchens">Number of Kitchens:</label>
                    <input type="number" id="kitchens" name="kitchens" value="<%= formData.kitchens %>" min="1">
                </div>
                <div>
                    <label for="yearBuilt">Year Built:</label>
                    <input type="text" id="yearBuilt" name="yearBuilt" value="<%= formData.yearBuilt %>">
                </div>
            </div>

            <!-- General Observations -->
            <h3>General Observations</h3>
            <div class="grid-container">
                <div>
                    <label for="plumbingCondition">Condition of Plumbing System:</label>
                    <textarea id="plumbingCondition" name="plumbingCondition"><%= formData.plumbingCondition %></textarea>
                </div>
                <div>
                    <label for="leakSigns">Signs of Leaks or Water Damage:</label>
                    <textarea id="leakSigns" name="leakSigns"><%= formData.leakSigns %></textarea>
                </div>
                <div>
                    <label for="waterPressure">Water Pressure (PSI):</label>
                    <select id="waterPressure" name="waterPressure">
                        <option value="High" <%= formData.waterPressure === 'High' ? 'selected' : '' %>>High</option>
                        <option value="Normal" <%= formData.waterPressure === 'Normal' ? 'selected' : '' %>>Normal</option>
                        <option value="Low" <%= formData.waterPressure === 'Low' ? 'selected' : '' %>>Low</option>
                    </select>
                </div>
            </div>

            <!-- Fixtures and Fittings -->

<div class="page-break">

            <h3>Fixtures and Fittings</h3>
            <div class="grid-container">
                <!-- Kitchen Fixtures -->
                <div class="checkbox-group">
                    <h4>Kitchen</h4>
                    <label>Sink:
                        <select name="kitchenSink">
                            <option value="Good" <%= formData.kitchenSink === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.kitchenSink === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.kitchenSink === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                    <label>Faucet:
                        <select name="kitchenFaucet">
                            <option value="Good" <%= formData.kitchenFaucet === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.kitchenFaucet === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.kitchenFaucet === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                    <label>Dishwasher:
                        <select name="kitchenDishwasher">
                            <option value="Good" <%= formData.kitchenDishwasher === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.kitchenDishwasher === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.kitchenDishwasher === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                </div>

                <!-- Bathroom Fixtures -->
                <div class="checkbox-group">
                    <h4>Bathroom</h4>
                    <label>Sink:
                        <select name="bathroomSink">
                            <option value="Good" <%= formData.bathroomSink === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.bathroomSink === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.bathroomSink === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                    <label>Faucet:
                        <select name="bathroomFaucet">
                            <option value="Good" <%= formData.bathroomFaucet === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.bathroomFaucet === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.bathroomFaucet === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                    <label>Toilet:
                        <select name="bathroomToilet">
                            <option value="Good" <%= formData.bathroomToilet === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.bathroomToilet === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.bathroomToilet === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                    <label>Shower/Tub:
                        <select name="bathroomShowerTub">
                            <option value="Good" <%= formData.bathroomShowerTub === 'Good' ? 'selected' : '' %>>Good</option>
                            <option value="Fair" <%= formData.bathroomShowerTub === 'Fair' ? 'selected' : '' %>>Fair</option>
                            <option value="Poor" <%= formData.bathroomShowerTub === 'Poor' ? 'selected' : '' %>>Poor</option>
                        </select>
                    </label>
                </div>
            </div>
</div>
<div class="no-print">
            <!-- Upload Pictures -->
            <div class="file-upload">
                <h3>Upload Inspection Pictures</h3>
                <label>Upload Picture 1:</label>
                <input type="file" name="picture1" accept="image/*">
                <label>Upload Picture 2:</label>
                <input type="file" name="picture2" accept="image/*">
                <label>Upload Picture 3:</label>
                <input type="file" name="picture3" accept="image/*">
            </div>
</div>

            <!-- Additional Notes -->
            <div class="section">
                <h3>Additional Notes</h3>
                <textarea name="additionalNotes" placeholder="Enter any additional notes here..."><%= formData.additionalNotes %></textarea>
            </div>

<div class="no-print">
            <!-- Submit Button -->
            <input type="submit" value="Submit Report">
        </form>
    </div>
</div>
</body>
</html> 
